# 程式註解（Code Comments）開發規則

目的
- 建立一致且可維護的程式註解規範，確保團隊間知識傳承、加速新進工程師上手、並降低維護成本。

適用範圍
- 專案中所有的程式碼檔案、模組、類別/物件、函式/方法、公開與內部 API、以及重要流程或演算法。

總則
1. 註解的目的為「說明為何（why）與做什麼（what）」，而非逐字敘述程式在做的每一行（how）—若程式太難理解，應重構而非大量註解。
2. 註解應簡潔、精確、可讀且易於搜尋。
3. 優先使用英文作為程式碼內註解語言；若專案或團隊偏好中文，可在檔頭或專案 README 明確說明。註解內若包含專有名詞或上下文，可同時以括號補充中文或英文對照。
4. 保持一致的風格（語氣、縮排、標點、命名引用），並遵循各語言既有的文件化格式（例如：Python 的 docstring、Java 的 Javadoc、C# 的 XML comments、JS 的 JSDoc）。
5. 註解要與程式碼同步：修改程式行為時，必須同步更新相關註解；過時註解比沒有註解更有害。

註解類型與具體規範

1) 檔案 / 模組層級註解
- 放置於檔案頂端，簡述檔案目的、包含的主要類別或函式、以及任何限制或外部相依性。
- 如有版權、授權或重要注意事項（例如安全性、隱私），應明確列出。

範例：
```text
// module: auth_service.js
// purpose: 提供使用者登入/登出與 JWT 相關功能
// notes: 依賴 redis 作為 session cache；token 失效邏輯在 tokenManager
```

2) 類別 / 物件註解
- 說明類別責任、主要成員、使用場景、生命週期與任何重要副作用。
- 標註可見性／使用者契約（public API）和 thread-safety 或非同步行為。

範例（Javadoc / XML / docstring 風格）：
```text
/**
 * UserService
 * 負責使用者 CRUD 與驗證流程
 * 注意：此類別不是 thread-safe，外層請自行同步
 */
```

3) 函式 / 方法註解（最重要）
- 必備欄位：功能簡述（summary）、參數（params）、回傳值（returns）、可能拋出的例外（throws/exceptions）、副作用（side effects）以及複雜度說明（如果有）。
- 若函式有前置條件（preconditions）或後置條件（postconditions），請明確列出。
- 當函式為 API 或 library 的一部分，提供使用範例（簡短）與邊界情形說明。

範例（通用模板）：
```text
/**
 * summary: 將輸入字串安全地轉為 HTML
 * params:
 *   - input: 原始字串
 * returns: 編碼後可用於 innerHTML 的字串
 * throws: 如果 input 為 null 會拋出 ArgumentNullException
 * example:
 *   const safe = escapeHtml(userInput);
 */
```

4) 變數註解
- 對於非自明、代表重要狀態或含有單位/範圍的變數，應補充註解。例如：時間以毫秒為單位、位元欄位的含意、magic number 的來源。
- 避免為非常自明變數（例如 i, j 的 loop index）撰寫冗長註解。

範例：
```text
// cacheExpiryMs: 快取失效時間，單位毫秒
const cacheExpiryMs = 300000;
```

5) 行內註解（Inline / End-of-line comments）
- 只用於補充單行或片段的意圖，應放在程式碼上方（preferred）或行尾（當短小且不影響可讀性）。
- 不要用行內註解掩蓋糟糕的設計；若註解變多表示該段程式應重構。

6) 區塊註解（Complex logic / Algorithm explanation）
- 對於複雜演算法或邏輯流程，提供步驟化說明、時間空間複雜度、可能的邊界情形，以及設計選擇的理由（例如為何選擇演算法A而非B）。
- 可帶上小圖示、偽程式或簡短範例以協助理解。

7) TODO / FIXME / NOTE 標記
- 使用一致的標記：TODO（待完成的工作）、FIXME（必須修復的 bug）、NOTE（重要說明）。
- 每項標記都必須包含責任人或指派者（例如 @alice）和期限或 issue 編號（如 #123）。
- 範例：
```text
// TODO(@bob): 支援多租戶 (#456) - 目標 2023-12-01
// FIXME: 修正 race condition 在 createOrder
```

風格與格式建議
- 每個函式註解至少一行 summary，summary 長度建議不超過 120 字。
- 參數與回傳說明採統一鍵詞（例如 params, returns, throws）以利工具擷取。
- 使用全形或半形標點需在專案約定中一致。
- 註解字句使用陳述句、主動語態與第三人稱現在式，例如「建立使用者紀錄」而非「建立了使用者紀錄」或「會建立使用者紀錄」。
- 避免在註解中寫出敏感資訊（密碼、金鑰、個資），必要時僅描述處理方式並指向安全文件。

實務範例（函式註解模板）
- Java / TypeScript (JSDoc)
```text
/**
 * summary: 將數值四捨五入到指定小數位
 * @param {number} value - 要四捨五入的數值
 * @param {number} digits - 小數位數
 * @returns {number} 四捨五入後的數值
 */
function round(value, digits) { ... }
```

- Python (docstring)
```text
def round_value(value: float, digits: int) -> float:
    """
    summary: 四捨五入數值

    Args:
        value: 要四捨五入的數值
        digits: 小數位數

    Returns:
        四捨五入後的數值
    """
```

維護流程與審查
- Code review 時，檢查註解是否完整且與實際行為一致。若註解過時請要求修改或提交修正。
- Pull request 必要時應在描述中指出對註解所作的變更。
- 使用靜態分析工具或 linter（例如 ESLint 的 jsdoc plugin、pydocstyle、DocFX/StyleCop 等）來自動驗證註解風格與必要欄位。

教育與知識傳承
- 在專案的入門文件（README 或新進手冊）包含註解風格指引摘要與範例。
- 定期在團隊會議或 code review 中分享註解最佳實踐與常見問題。

附錄：快速檢查清單（PR 評審時）
- 是否存在檔案/模組層級說明？
- 公開或重要 API 是否有詳細函式註解（參數、回傳、例外）？
- 複雜邏輯是否有演算法或流程描述？
- TODO/FIXME 是否有負責人與 issue 連結？
- 註解語言是否與專案約定一致？
- 註解是否與程式碼同步（無過時或矛盾描述）？

結語
- 註解不是替代清晰程式碼的工具；良好命名與模組化應為首選。
- 註解應補足程式碼無法直接表達的設計決策、風險及使用契約，並作為團隊知識傳承的主要載體。

> 文件維護：此檔案為專案註解規範，若需調整請以 PR 方式修訂並在變更說明中註明原因與影響範圍。

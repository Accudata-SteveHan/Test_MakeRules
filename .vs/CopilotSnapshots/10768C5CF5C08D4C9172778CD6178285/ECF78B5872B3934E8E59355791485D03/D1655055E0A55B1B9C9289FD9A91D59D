# CONTRIBUTING_ERRCONTROL.md

## 例外處理與錯誤碼規範

本文件定義專案中例外處理、錯誤碼、日誌與可觀測性的統一規範，確保系統具有可預測的故障處理、易追蹤的錯誤路徑，以及生產環境中的可維護性與可診斷性。

---

## 1. 例外處理與錯誤碼規範

### 1.1 核心原則（精簡）

- 預期錯誤（業務驗證、輸入錯誤等）使用結構化 Result/ErrorCode 回傳，**不以例外作為業務流控制**。
- 非預期例外（系統錯誤、未預期執行時例外）必須拋出並傳播到可集中處理的邊界（API 層或全域中介）。
- 任何捕捉到的 Exception 都不得吞掉：必須記錄（依層級規範）並重新拋出（throw 或包裝後 throw），以維持可追蹤性。
- 日誌與 ErrorCode 的決定權依層級：DAL/Infra 轉換為系統/外部錯誤碼或直接 rethrow；Application 可做重試/降級後 rethrow；Presentation 層負責與用戶回應映射。

---

### 1.2 結構化錯誤碼（摘要）

- 使用枚舉或常數定義 ErrorCode（範例分區：BIZ 1000-1999、EXT 2000-2999、SYS 3000-3999）。
- 錯誤碼對 HTTP 狀態碼的映射：BIZ → 4xx，EXT → 502/503/504，SYS → 5xx。
- 推薦使用 Result<T> 封裝輸出，ErrorInfo 含 Code/Message/Details/CorrelationId。

---

### 1.3 精簡範例（每項目短小示例）

1) 錯誤做法：吞例外（禁止）

```csharp
try {
    await _repo.SaveAsync(entity);
} catch (Exception) { /* 空置：禁止 */ }
```

2) 正確做法：記錄後重新拋出（Presentation 層統一處理）

```csharp
try {
    await _repo.SaveAsync(entity);
} catch (DbUpdateException ex) {
    _logger.LogError(ex, "DB save failed {OrderId}", order.Id);
    throw; // 必須 rethrow
}
```

3) 如果層級選擇用 Result 回傳（業務驗證）

```csharp
if (!valid) return Result.Error(ErrorCode.BIZ_VALIDATION_REQUIRED_FIELD, "missing");
```

4) 可接受的恢復策略：嘗試重試後仍失敗需拋出

```csharp
int attempts = 0;
while (attempts++ < 3) {
  try { await _repo.SaveAsync(entity); break; }
  catch (TransientException ex) when (attempts < 3) { await Task.Delay(100); }
  catch (Exception ex) { _logger.LogError(ex, "save failed"); throw; }
}
```

說明：每個 catch 分支要麼做恢復，要麼記錄並 rethrow；絕不 silent-fail。

---

### 1.4 層級處理原則（簡潔）

- DAL/Infrastructure：捕捉可辨識的外部錯誤（連線失敗、超時），可轉換為 EXT/SYS ErrorCode 並記錄，然後 rethrow 或包裝 `new ExternalServiceException(code, ex)` 再 throw。
- Application：處理業務協調、可在內部做重試/降級，若無法恢復則記錄並 rethrow 或回傳 Result（若為預期業務錯誤）。
- Presentation/API：最終捕捉所有未處理例外，記錄（含 CorrelationId），並回傳統一 API 回應（對外不要洩露 stack trace）。

---

### 1.5 檢查清單（必做）

- [ ] 所有 catch 塊要麼恢復（explicit），要麼記錄並 rethrow。
- [ ] 禁止空的 catch 或吞例外。
- [ ] 預期的業務錯誤使用 Result/ErrorCode，不以例外控制流程。
- [ ] 每個錯誤碼有唯一且清晰定義。
- [ ] 例外日誌含 CorrelationId 與足夠上下文，但不含敏感資訊。
- [ ] Presentation 層負責將例外映射到安全的 HTTP 回應。

---

## 2. 日誌與可觀測性規範（精簡）

### 2.1 原則要點

- 使用結構化日誌（JSON / key-value），所有日誌包含至少：CorrelationId、Module、Action、Level、Timestamp、ErrorCode（若適用）。
- 生產環境預設最低等級 Info；Error/Critical 必須記錄例外堆疊資訊。
- 禁止記錄敏感資料（密碼、token、完整個資、信用卡等）。
- 避免在熱路徑大量記錄；在需要時使用 IsEnabled 檢查。

### 2.2 日誌等級（簡短）
- Critical：系統崩潰、不可恢復。
- Error：操作失敗、需關注。
- Warn：潛在問題或退化行為。
- Info：業務重要事件。
- Debug/Trace：開發/診斷用，生產關閉。

### 2.3 最小範例（記錄 + 傳播 CorrelationId）

Middleware 生成 CorrelationId 並加入日誌 scope；例外 catch 範例：

```csharp
_logger.LogError(ex, "Error in CreateOrder {OrderId} - Correlation:{CorrelationId}", order.Id, correlationId);
throw; // 必須 rethrow
```

---

## 3. 其他注意事項（簡短）

- 第三方套件例外：文件化其可能丟出的例外並在呼叫點做明確處理（記錄並 rethrow 或轉換為 ErrorCode）。
- 非同步上下文：避免用 .Result 或 .Wait() 阻塞；例外在 async/await 中同樣必須 rethrow 或回傳 Result。

---

## 4. 參考資源
- .NET Exception Handling Best Practices (Microsoft)
- Serilog docs
- Microservices Patterns - Correlation ID

##CONTRIBUTING_VARIABLE.md

## 目的
本文件定義專案的貢獻準則與開發風格規範，確保程式碼一致性、可維護性與可讀性。所有貢獻者（包括外部 PR）應遵守下列規範。

## 總則
- 使用強型別（strong typing）為預設原則。
- 非必要請勿使用 `var`、`dynamic` 或其他會造成型別不明或弱型別的語法。
- 所有公有 API、類別欄位、屬性、方法簽章與介面必須使用具名型別。

## 強型別規範（必須遵守）
1. 明確型別
   - 在所有的成員（`private`, `public`, `internal`, `protected`）與方法回傳值及參數上，務必使用具名型別（例如 `int`, `string`, `MyType`）。
   - 在本地變數宣告上，預設使用具名型別。

2. `var` 的例外情況（允許但需註明理由）
   - 匿名型別（如 `new { ... }`）必須使用 `var`。
   - 在 LINQ 查詢中接收匿名結果時可使用 `var`。
   - 右側顯性的型別表達時（例如 `var x = new MyType(...);`）可接受，但推薦改為 `MyType x = new MyType(...);` 除非 `var` 明顯提升可讀性，且必須在 PR 描述或註解中說明理由。
   - 對於極短作用域的暫時性變數，若使用 `var` 明顯改善可讀性，需在程式碼附近加上簡短註解說明。

3. `dynamic` 的使用
   - 嚴格禁止在一般業務邏輯或核心程式碼中使用 `dynamic`。
   - 只有在與 COM、反射、或第三方動態介面整合且無可行替代方案時，才可使用 `dynamic`，並且必須在程式碼中加上詳細註解說明為何必要。

4. 程式碼審查規範
   - PR 審查員應檢查是否違反強型別原則。若發現未經允許使用 `var` 或 `dynamic`，應要求修正或提供充分理由。

## 命名與風格
- 請遵循專案的 .editorconfig 與 C# 命名慣例。
- 類別、介面、方法命名應具描述性且遵守 PascalCase / camelCase 規則。

## 變數使用規範
本節定義變數宣告與使用相關的規範，包含一般程式碼與函式參數的處理原則。所有貢獻者應遵守下列規定並在必要時取得專案管理人員同意。

1. 全域變數
   - 除非專案管理人員明確定義或同意，一般狀況下不可定義全域變數。全域變數會增加程式耦合性與維護成本，應優先考慮使用區域變數、注入（dependency injection）或傳遞參數的方式。
   - 若確實需要全域變數，請提出 RFC/Issue，說明使用理由、範圍、生命週期與替代方案，並取得專案管理人員的書面同意後才可實作。

2. 函式參數預設值
   - 除非專案管理人員明確定義或同意，一般狀況下不可在函式參數上設定預設值。預設值可能在不同呼叫情況下造成隱性行為，增加 API 維護負擔。
   - 若確需提供預設行為，優先考慮使用方法重載、工廠方法或顯式建立設定物件（options object），並在 PR 中說明為何必須使用預設參數。

3. 變數範圍提升與重構流程
   - 如果認為提升變數的生命週期或使用範圍（例如由區域提升為類別成員）對未來專案發展有利，請先提出具體建議（Issue/RFC），包含優點、風險、影響範圍與範例程式碼。
   - 經專案管理人員審核並同意後，方可在相應的 PR 中進行變數重構，並在 PR 描述中註明已取得的同意與相關討論連結。
   - 在重構過程中，請確保有完整的單元測試或整合測試覆蓋受影響的行為，並儘量將變更限定在最小影響範圍內以降低風險。

本節旨在降低隱性副作用與提升程式可維護性。如有特殊需求請依專案流程提出討論，取得管理層同意後再實作。



# CONTRIBUTING_LOGGING.md

## 目的
本文件定義專案的日誌（logging）實作與貢獻準則，包含「API / 交易日誌」與「偵錯 / 開發用日誌」的使用情境、格式、等級與保留政策，確保日誌可供運維、監控、稽核與除錯使用且不違反資安規範。

## 適用範圍
適用於本專案所有伺服端服務、API 層、背景工作程序與共用函式庫。新進開發者需遵守本文件規範，PR 審查時亦會檢查日誌相關實作。

## 日誌類型
1. API / 交易日誌（Operational / Audit）
   - 目的：記錄對外或系統關鍵交易、狀態變更、審計事件與可追蹤的錯誤。
   - 特性：結構化（JSON）為主、長期保存或按合規需求保存、不得包含未遮罩的敏感資訊、必含 correlation id。

2. 偵錯日誌（Debug / Trace）
   - 目的：協助開發與本地化除錯，記錄詳細流程、變數與執行緒/堆疊等資訊。
   - 特性：高詳盡度、預設在開發/測試環境啟用，生產環境預設關閉或僅短期啟用。

## 日誌等級
建議使用標準等級：Trace < Debug < Information < Warning < Error < Critical。等級使用原則：
- Production 預設至少為 `Information` 或 `Warning`。
- 調查問題時可臨時提升至 `Debug/Trace`，需有開關與保留策略。

## 結構化日誌欄位
日誌應以結構化 JSON 為主，至少包含：
- `timestamp`：ISO 8601 時間戳
- `level`：日誌等級
- `service`：服務名稱或模組
- `environment`：production/staging/dev
- `correlation_id` 或 `request_id`
- `message`：描述文字
- `meta`：物件，放置額外結構化欄位（如 user_id、order_id 等）

若使用分散式追蹤，應包含 `trace_id` / `span_id`。

## Correlation ID 與追蹤
- 每個外部請求應生成或透傳 `correlation_id`（例如 HTTP header `X-Correlation-ID`）。
- 所有與該請求相關的日誌必須攜帶相同 `correlation_id`，以利串接與查詢。

## 敏感資訊處理
- 禁止在日誌中寫入明文密碼、金鑰、完整身分識別資訊或信用卡號等敏感資料。
- 必須在寫入前遮罩或省略敏感欄位（例如 `**** **** **** 1234`），必要時申請授權並記錄保留理由與時限。

## 保留與儲存策略
- API / 交易日誌：視合規至少保存 30 天或依公司政策延長。
- 偵錯日誌：在生產環境應短期保存或不儲存詳細 Debug 資訊。
- 建議使用集中式日誌平台（ELK、Splunk、Datadog、或雲端 provider）並啟用索引與輪替。

## 設定與開關管理
- 日誌等級透過設定檔或環境變數控制（例 `LOG_LEVEL`、`ASPNETCORE_ENVIRONMENT`）。
- 生產環境禁止直接在程式碼中硬編 `Debug/Trace` 為預設；需使用集中設定或 feature flag 控制。

## 技術選型建議
- .NET：使用 `Microsoft.Extensions.Logging`（ILogger）搭配 Serilog（輸出 JSON）。
- Node.js：使用 `pino` 或 `winston` 並輸出結構化 JSON。
- Java：使用 SLF4J + Logback 並啟用 JSON encoder。

## 實作範例（C#）
1. 在 middleware 保證每請求有 `correlation_id`：

```csharp
// 於 middleware 中：
var correlationId = context.Request.Headers["X-Correlation-ID"].FirstOrDefault() ?? Guid.NewGuid().ToString();
using (logger.BeginScope(new Dictionary<string, object>{{"correlation_id", correlationId}}))
{
    await _next(context);
}
```

2. 記錄 Error 時包含上下文但不得包含敏感資訊：

```csharp
try
{
    // ...
}
catch (Exception ex)
{
    logger.LogError(ex, "Failed to process order {OrderId}", orderId);
    throw;
}
```

## 開發者準則（何時記錄）
- Information：記錄重要事件（請求開始/結束、主要流程成功）且不包含大量資料。
- Warning：非阻斷性異常或可能造成問題的情境。
- Error / Critical：異常或系統不可用情況，應附必要上下文與 correlation id。
- Debug / Trace：僅供開發/測試使用，生產環境不可長期啟用。

## 審查重點與禁止事項
- PR 審查應確認：等級使用恰當、無敏感資料、包含 correlation_id、輸出為結構化格式。
- 禁止在生產日誌中寫入明文密碼、金鑰或完整個人資料。 
- 禁止在高頻路徑做大量同步 I/O 日誌呼叫（導致效能問題）。
- 日誌不可替代 metrics，重要量測需上報 metrics 系統。

## 變更流程與聯絡
- 若需新增日誌欄位或調整保留策略，請提交 RFC 並在 PR 中說明影響（存量、索引成本、合規影響）。
- 日誌相關重大變更應由資深工程師與運維共同審核。

---

最後更新：2026-03-17


